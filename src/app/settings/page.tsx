import { PlaceholderWorkspace } from "@/components/layout/placeholder-workspace";

export default function SettingsPage() {
  return (
    <PlaceholderWorkspace
      title="Settings"
      description="Supabase auth, API keys, and workspace preferences will live here."
    />
  );
}
