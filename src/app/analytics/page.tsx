import { PlaceholderWorkspace } from "@/components/layout/placeholder-workspace";

export default function AnalyticsPage() {
  return (
    <PlaceholderWorkspace
      title="Analytics"
      description="Charts for bids, reply rate, win rate, and platform mix ship with the analytics milestone—this route is wired for that module."
    />
  );
}
