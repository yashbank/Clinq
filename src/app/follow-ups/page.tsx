import { PlaceholderWorkspace } from "@/components/layout/placeholder-workspace";

export default function FollowUpsPage() {
  return (
    <PlaceholderWorkspace
      title="Follow-ups"
      description="Ghost detection, timing suggestions, and short follow-up drafts—assistant-driven reminders only."
    />
  );
}
